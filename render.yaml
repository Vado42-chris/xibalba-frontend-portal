services:
  - type: web
    name: xibalba-frontend-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /
    autoDeploy: true
    branch: main
    rootDir: .
